{
  "name": "@clusterws/cws",
  "version": "0.17.2",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "ClusterWS cWS custom fork of uWebSockets 0.14",
  "scripts": {
    "install": "npm run build-bindings",
    "build-bindings": "node-gyp rebuild > build_log.txt 2>&1 || exit 0",
    "test": "mocha -r ts-node/register './tests/**/*.test.ts' --exit",
    "build": "ts-builder -c ./tsbuild.json",
    "clean": "rimraf dist/"
  },
  "engines": {
    "node": ">=8.12.0"
  },
  "keywords": [
    "tiny",
    "websocket",
    "clusterws",
    "cws"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.2.1",
    "@types/mocha": "^5.2.7",
    "@types/node": "^10.7.0",
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "rimraf": "^3.0.0",
    "ts-builder": "0.9.0",
    "ts-node": "^8.5.4"
  }
}
