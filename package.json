{
  "name": "@clusterws/uws",
  "version": "0.4.8",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "ClusterWS uWS modified version of uWebSockets",
  "scripts": {
    "install": "node-gyp rebuild > build_log.txt 2>&1 || exit 0",
    "build": "ts-builder -c ./tsbuild.json"
  },
  "engines": {
    "node": ">=8"
  },
  "keywords": [
    "tiny",
    "websocket",
    "clusterws",
    "uws"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.7.0",
    "ts-builder": "0.8.0"
  }
}
