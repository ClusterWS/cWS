{
  "name": "clusterws-uws",
  "version": "0.4.6",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "install": "node-gyp rebuild > build_log.txt 2>&1 || exit 0",
    "build": "ts-builder -c ./tsbuild.json"
  },
  "description": "ClusterWS uWS modified verstion of uWebSockets",
  "engines": {
    "node": ">=8"
  },
  "keywords": [
    "tiny",
    "websockets",
    "clusterws",
    "broker"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.7.0",
    "ts-builder": "0.7.4"
  }
}