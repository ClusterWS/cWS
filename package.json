{
  "name": "clusterws-uws",
  "version": "0.0.6",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "install": "node-gyp rebuild > build_log.txt 2>&1 || exit 0",
    "build": "clusterws-builder -src=lib -type=node -npm -no_assets"
  },
  "description": "ClusterWS uWS modified module of uWebSockets",
  "engines": {
    "node": ">=8"
  },
  "keywords": [
    "tiny",
    "websockets",
    "clusterws",
    "broker"
  ],
  "license": "MIT",
  "devDependencies": {
    "clusterws-builder": "^0.1.2"
  }
}
