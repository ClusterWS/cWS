{
  "name": "@clusterws/cws",
  "version": "0.10.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "ClusterWS cWS modified version of uWebSockets",
  "scripts": {
    "install": "node-gyp rebuild > build_log.txt 2>&1 || exit 0",
    "build": "ts-builder -c ./tsbuild.json"
  },
  "engines": {
    "node": ">=8"
  },
  "keywords": [
    "tiny",
    "websocket",
    "clusterws",
    "cws"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.7.0",
    "ts-builder": "0.8.0"
  }
}